# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 概要

プロジェクトの概要・構成・ソース一覧は README.md を参照。

## 注意点

- ソース定義の追加・編集時は STYLEGUIDE.md に従う
- cache/ と output/ は gitignore 対象。コミットしない
- 日本語でコミュニケーションする
- この repo で作業するときの環境メモ（ビルド・描画確認・検証コマンド・CI の癖）は [.claude/notes/](.claude/notes/) にある。実行環境はセッションごとに作り直されるため、ここに書いてあることを毎回調べ直さない。調べ直した結果が違っていたら更新する
